<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:vm="clr-namespace:SunshineMauiMessaging.ViewModels"
             x:Class="SunshineMauiMessaging.Pages.ActivitiesPage"
             Title="Activities"
             x:DataType="vm:ActivitiesPageViewModel">
    <VerticalStackLayout>
        <!-- Padding="{OnPlatform iOS='30,60,30,30', Default='30'}"-->
        <!--<VerticalStackLayout.Padding>
            <OnPlatform x:TypeArguments="Thickness">
                <On Platform="iOS" Value="30,60,30,30" />
                <On Platform="Android" Value="30" />
                <On Platform="WinUI" Value="30" />
            </OnPlatform>
        </VerticalStackLayout.Padding>-->
        <Label 
            Text="Welcome to .NET MAUI!"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />
        <Entry Text="{Binding ExampleText}" WidthRequest="200"/>
        <Button Command="{Binding OkClickedCommand}" Text="Ok"/>
    </VerticalStackLayout>
</ContentPage>